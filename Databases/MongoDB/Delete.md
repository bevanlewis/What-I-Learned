# Delete

## delete documents

Delete one element, uses find sytax for selecting the element.

```shell
db.collection.deleteOne({})
```

Delete many element

```shell
db.collection.deleteMany({})
```

The above command deletes all documents in the collection.
