# Insert

## insert documents

```shell
db.collection.insertOne()
db.collection.insertMany()
db.collection.insert() # insert a single document or multiple documents
```

`insertOne()` expects an object
`insertMany()` expects and array of objects
